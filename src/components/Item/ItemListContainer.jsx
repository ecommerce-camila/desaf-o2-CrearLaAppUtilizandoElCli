import React from 'react'

const ItemListContainer = (props) => {
    return (
        <>
            <h1>{props.greeting}ItemListContainer</h1>
        </>
    );
};

export default ItemListContainer;
